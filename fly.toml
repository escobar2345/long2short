# fly.toml app configuration file generated for long2short-u0dnza on 2026-09-09T13:26:09Z
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'long2short-u0dnza'
primary_region = 'ams'

[build]
  dockerfile = 'Dockerfile'

[env]
  NEXT_TELEMETRY_DISABLED = '1'
  NODE_ENV = 'production'

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

[[vm]]
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 2048
